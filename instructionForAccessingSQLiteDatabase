use the command

	sqlite3 dbfile

where dbfile is the name of the database file you want to view/edit.

then just start typing in sql database commands.

	create table tbl1(one text, two int);
	insert into tbl1 values('hello', 10);

	select * from tbl1;

	CTRL-D

CTRL-D exits the program.

references:
https://www.sqlite.org/cli.html
